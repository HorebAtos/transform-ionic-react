import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.transform-ionic-react',
  appName: 'TransformIonicReact',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
